import makeStyles from '@mui/styles/makeStyles'

export default makeStyles(theme => ({
  eventImage: {
    width: '100px',
    height: '100px',
    borderRadius: '12px',
    marginBottom: '24px',
  },
}))
